{
  "version": 2,
  "buildCommand": "npm run build && mkdir -p api/dist && cp -r dist/* api/dist/",
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}

